import 'package:flutter/material.dart';

class GreenFluxApp extends StatelessWidget {
  const GreenFluxApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
